name: Temporary CSS Refinement on Main

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  refine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Validate runner
        run: echo "temporary CSS refinement runner ready"
